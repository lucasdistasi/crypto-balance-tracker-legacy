spring:
  data:
    mongodb:
      uri: mongodb://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@localhost:27017
      database: ${MONGODB_DATABASE}

coingecko:
  url: "https://api.coingecko.com/api/v3"

springdoc:
  swagger-ui:
    defaultModelsExpandDepth: -1